#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mqtt-ws/bin/mqttwsBridge.js" "$@"
  ret=$?
else 
  node  "$basedir/../mqtt-ws/bin/mqttwsBridge.js" "$@"
  ret=$?
fi
exit $ret
